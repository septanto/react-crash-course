export default function NewsNotFoundPage() {
    return (
        <div id="error">
            <h1>Not Found!</h1>
            <p>Unfortunately, we can't find the requested article.</p>
        </div>
    )
}
